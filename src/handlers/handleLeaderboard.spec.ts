import test from 'ava'

import { handleLeaderboard } from './handleLeaderboard.js'

/*
test('handles a leaderboard', t => {
  t.is(handleLeaderboard(), '')
})
*/

test('TODO', t => {
  t.is(true, true)
})
